"""AI-assisted platform features (Anthropic integration).

The entire auto-tagging flow — prompts, the data sent to Anthropic, validation,
retry, and merge — lives in :mod:`openfdd_stack.platform.ai.tagging`. Read that
one module to audit exactly what leaves the platform and what comes back.
"""
