# Vite loads this for `vite build` in production mode (overrides .env).
# In SWA deploys the SPA is same-origin with /api/* proxied to the linked ACA backend.
# Keep .env pointing at the lab/dev host (localhost:8000) for local development.
VITE_API_BASE=/api
