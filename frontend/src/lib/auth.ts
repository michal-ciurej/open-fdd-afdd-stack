// Legacy in-process token store removed.
// Auth now flows through SWA-injected Entra principals; see @/contexts/auth-context.
// This file is intentionally empty and exists only to avoid breaking historical imports
// during the migration. Delete once nothing references it.
export {};
